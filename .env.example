# Configurações de Ambiente - Fretamento Intertouring
# Copie este arquivo para .env e configure as variáveis conforme seu ambiente

# ============================================
# CONFIGURAÇÕES BÁSICAS
# ============================================

# Ambiente (development, staging, production)
DJANGO_ENV=development

# Chave secreta do Django (gere uma nova para produção)
DJANGO_SECRET_KEY=django-insecure-!ybpw6s0nsg=d_j@n(4aj7z1tu2ok&y99r_$1&t-2&%xst-9p)

# Debug (False para produção)
DEBUG=True

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=127.0.0.1,localhost,testserver

# ============================================
# BANCO DE DADOS
# ============================================

# Configurações do PostgreSQL (para produção)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=fretamento_prod
DB_USER=fretamento_user
DB_PASSWORD=senha_super_forte_aqui
DB_HOST=localhost
DB_PORT=5432

# Para desenvolvimento com SQLite, deixe DB_ENGINE vazio
# DB_ENGINE=

# ============================================
# CACHE E REDIS
# ============================================

# URL do Redis (para cache e sessões)
REDIS_URL=redis://127.0.0.1:6379/1

# ============================================
# EMAIL
# ============================================

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-de-app
DEFAULT_FROM_EMAIL=noreply@seu-dominio.com

# ============================================
# ARQUIVOS E STORAGE
# ============================================

# AWS S3 (opcional)
USE_S3=False
AWS_ACCESS_KEY_ID=sua-aws-access-key
AWS_SECRET_ACCESS_KEY=sua-aws-secret-key
AWS_STORAGE_BUCKET_NAME=seu-bucket-s3
AWS_S3_REGION_NAME=us-east-1

# ============================================
# SEGURANÇA
# ============================================

# Configurações HTTPS (True para produção)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0

# Domínios confiáveis para CSRF (separados por vírgula)
CSRF_TRUSTED_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com

# Timeout de sessão em segundos (30 minutos = 1800)
SESSION_TIMEOUT=1800

# ============================================
# MONITORAMENTO
# ============================================

# Sentry DSN (para monitoramento de erros)
SENTRY_DSN=

# ============================================
# LOGS
# ============================================

# Diretório para logs
LOG_DIR=/var/log/fretamento

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO